package com.example.apnakirana.data.model

data class OnboardingPage(
    val title: String,
    val description: String,
    val image: String // We'll use emoji for now, can replace with actual images later
)